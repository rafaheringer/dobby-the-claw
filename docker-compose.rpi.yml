services:
  bridge:
    # Raspberry Pi (ARM64) override. All device/group_add/volume/GStreamer settings
    # already come from the base docker-compose.yml; duplicating group_add/devices here
    # breaks Compose validation ("items 0 and 1 are equal"), so keep this file minimal.
    platform: linux/arm64
